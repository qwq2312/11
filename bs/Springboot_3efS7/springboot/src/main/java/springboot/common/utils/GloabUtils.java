package springboot.common.utils;import cn.dev33.satoken.stp.StpUtil;import springboot.constants.Constants;public class GloabUtils {	//小于3的是管理员	public static final int ROLE_ADMIN_Limit = 3;	public static TokenEntity getUserInfo() {		return (TokenEntity) StpUtil.getSession().get(Constants.CURRENT_USER);	}	public static Long getUserId() {		TokenEntity tokenEntity = (TokenEntity) StpUtil.getSession().get(Constants.CURRENT_USER);		if (null != tokenEntity) {			return tokenEntity.getUserid();		} else {			return 1L;		}	}	/**	 * @return boolean	 * @title: isAdmin	 * @Description //判断是否是admin	 * @Param []	 **/	public static boolean isAdmin() {		if (null != getUserInfo()) {			return Integer.parseInt(getUserInfo().getRole()) < ROLE_ADMIN_Limit;		} else {			return false;		}	}	/**	 * @return boolean	 * @title: isNotAdmin	 * @Description //判断是否不是admin用户	 * @Param []	 **/	public static boolean isNotAdmin() {		if (null != getUserInfo()) {			boolean isAdmin = Integer.parseInt(getUserInfo().getRole()) < ROLE_ADMIN_Limit;			return !isAdmin;		} else {			return false;		}	}}