package springboot.common.utils;import cn.hutool.core.util.StrUtil;import cn.hutool.crypto.SecureUtil;import lombok.experimental.UtilityClass;@UtilityClasspublic class PwdUtil {	private static final String salt = "springbootStudy";	public static String encrypt(String str) {		if (StrUtil.isEmpty(str)) {			return "";		}		// 第一步: 2次MD5		String step1 = SecureUtil.md5(SecureUtil.md5(str));		// 第二步: 拼接salt		String step2 = salt + step1 + salt;		// 第三步: SHA256		return SecureUtil.sha256(step2);	}	/**	 * @return java.lang.String	 * @title: encrypt	 * @Description //自定义盐	 * @Param [str, cusSalt]	 **/	public static String encrypt(String str, String cusSalt) {		if (StrUtil.isEmpty(str)) {			return "";		}		if (StrUtil.isBlank(cusSalt)) {			cusSalt = salt;		}		// 第一步: 2次MD5		String step1 = SecureUtil.md5(SecureUtil.md5(str));		// 第二步: 拼接salt		String step2 = cusSalt + step1 + cusSalt;		// 第三步: SHA256		return SecureUtil.sha256(step2);	}}